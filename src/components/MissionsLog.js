import React from 'react';

const MissionsLog = (props) => {
    console.log(props.houseName);
    console.log(props.homeId);
    return (
        <div>
            <h1>Mission log</h1>

        </div>
    );
};

export default MissionsLog;